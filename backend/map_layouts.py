# backend/map_layouts.py

# 🥷 第壱区画：はじまりの島（19マス・標準的な六角形）
STAGE_01_BEGINNER = [
    (0, 0), # 中心
    (1, -1), (1, 0), (0, 1), (-1, 1), (-1, 0), (0, -1), # 半径1の輪（6マス）
    (2, -2), (2, -1), (2, 0), (1, 1), (0, 2), (-1, 2), 
    (-2, 2), (-2, 1), (-2, 0), (-1, -1), (0, -2), (1, -2) # 半径2の輪（12マス）
]

# 🥷 第弐区画：火山島（24マス・中央が空洞のドーナツ型）
STAGE_02_VOLCANO = [
    # ⚠️ (0, 0) は火山（内海）のため存在しない！
    
    # 半径1の輪（6マス）
    (1, -1), (1, 0), (0, 1), (-1, 1), (-1, 0), (0, -1), 
    
    # 半径2の輪（12マス）
    (2, -2), (2, -1), (2, 0), (1, 1), (0, 2), (-1, 2), 
    (-2, 2), (-2, 1), (-2, 0), (-1, -1), (0, -2), (1, -2),
    
    # 半径3の輪の一部（外側に少し拡張して合計24マスにする）
    # ※ここでは原作のやや縦長/横長な形状を表現するため、6方向に1マスずつ出っ張らせます
    (3, 0), (0, 3), (-3, 3), (-3, 0), (0, -3), (3, -3) 
]

# 🥷 第参区画：けいこく（28マス・4つの完全に独立した島）
# 各島は7マス（中心+周囲6マス）で構成され、互いに1マスの空洞（海）で隔てられています
STAGE_03_CANYON = [
    # 北西の島 (中心: 0, -3)
    (0, -3), (1, -4), (1, -3), (0, -2), (-1, -2), (-1, -3), (0, -4),
    # 東の島 (中心: 3, 0)
    (3, 0), (4, -1), (4, 0), (3, 1), (2, 1), (2, 0), (3, -1),
    # 南東の島 (中心: 0, 3)
    (0, 3), (1, 2), (1, 3), (0, 4), (-1, 4), (-1, 3), (0, 2),
    # 西の島 (中心: -3, 0)
    (-3, 0), (-2, -1), (-2, 0), (-3, 1), (-4, 1), (-4, 0), (-3, -1)
]

# 🥷 マップIDから座標リストを呼び出すための辞書（カタログ）
MAP_CATALOG = {
    "STAGE_01_BEGINNER": STAGE_01_BEGINNER,
    "STAGE_02_VOLCANO": STAGE_02_VOLCANO,
    "STAGE_03_CANYON": STAGE_03_CANYON  # 🥷 ここに追加！
}

def get_map_layout(map_id: str) -> list:
    """指定されたマップIDの座標リストを返す。未実装なら1面を返す。"""
    return MAP_CATALOG.get(map_id, STAGE_01_BEGINNER)